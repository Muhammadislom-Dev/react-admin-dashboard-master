import { useMutation } from "react-query";
import { API } from "../api";

export const usePhotoUpload = ({ setData }) => {
  //   const { mutate, data } = useMutation(async (payload) => {
  //     console.log(payload);
  //     return await API.fileUpload(payload)
  //       .then((res) => {
  //         console.log(res.data, setData);
  //       })
  //       .catch((err) => console.log(err));
  //   });
  //   return { mutate, data };
};
